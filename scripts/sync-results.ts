// Phase 1/6: sync match results from football-data.org
// Triggered via admin panel server action (Phase 6) or run manually

async function main() {
  console.warn("sync-results.ts: not yet implemented — Phase 1/6");
}

main().catch((err) => {
  console.error(err);
  process.exit(1);
});
