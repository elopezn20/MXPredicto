// Phase 1: seed teams, rounds, and matches from football-data.org
// Run with: npx tsx scripts/seed.ts

async function main() {
  console.warn("seed.ts: not yet implemented — Phase 1");
}

main().catch((err) => {
  console.error(err);
  process.exit(1);
});
